pluginManagement {
    includeBuild("build-logic")
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)

    repositories {
        google()
        mavenCentral()
        jcenter()
        maven { url 'https://jitpack.io' }
    }
}

include(":app")

include(":data")
include(":presentation")
include(":service-music")
include(":service-floating")

include(":core")
include(":analytics")
include(":equalizer")
include(":offline-lyrics")
include(":jaudiotagger")
include(":media")
include(":image-provider")
include(":app-shortcuts")

include(":intents")
include(":shared-android")
include(":prefs-keys")
include(":test-shared")
include(":shared")
include(":shared-widgets")

gradle.ext.exoplayerRoot = 'exoplayer'
gradle.ext.exoplayerModulePrefix = 'exoplayer-'
apply from: new File('buildscripts', 'exoplayer.gradle')