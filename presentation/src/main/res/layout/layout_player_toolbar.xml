<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/player_toolbar"
    android:clickable="true"
    android:focusable="true">

    <TextView
        android:id="@+id/nowPlaying"
        android:textSize="@dimen/player_now_playing_size"
        android:text="@string/player_now_playing"
        android:textStyle="bold"
        android:layout_marginStart="48dp"
        android:stateListAnimator="@animator/now_playing"
        android:textColor="@color/text_color_primary"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <ImageButton
        android:id="@+id/floatingWindow"
        android:layout_gravity="end"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/vd_bird_singing_24dp"
        android:tint="#424242"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:clickable="true"
        android:focusable="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/favorite"
        android:scaleX=".85"
        android:scaleY=".85"/>

    <dev.olog.presentation.widgets.LottieFavorite
        android:id="@+id/favorite"
        android:layout_width="wrap_content"
        tools:layout_width="48dp"
        android:layout_height="0dp"
        android:layout_gravity="end"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/playingQueue"/>

    <ImageButton
        android:id="@+id/playingQueue"
        android:layout_gravity="end"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/vd_playing_queue"
        android:tint="#424242"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:clickable="true"
        android:focusable="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>


</android.support.constraint.ConstraintLayout>