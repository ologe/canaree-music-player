package dev.olog.intents

object MusicConstants {

    private const val TAG = "MusicConstants"

    // used to adjust rpc delay
    const val STATE_EMISSION = "${TAG}_emission_time"

    const val PATH = "${TAG}_PATH"
    const val IS_PODCAST = "${TAG}_extra_is_podcast"
    const val SKIP_NEXT = "${TAG}_extra_skip_next"
    const val SKIP_PREVIOUS = "${TAG}_extra_skip_previous"

}