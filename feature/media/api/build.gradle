plugins {
    id("dev.olog.msc.library")
    id("dev.olog.msc.hilt")
}

android {
    namespace 'dev.olog.feature.media.api'
}

dependencies {
    implementation(projects.core)
    implementation(projects.shared)
    implementation(projects.sharedAndroid)

    implementation lib_x.media
    implementation lib_x.appcompat
}
